# Results

Generated JSON/CSV/Parquet outputs are ignored by default.

A result should be committed only when it is deliberately frozen as evidence and accompanied by its dataset version, configuration, code commit, and evidence level (E0–E4).
